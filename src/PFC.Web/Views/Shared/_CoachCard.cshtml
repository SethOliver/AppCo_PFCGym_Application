@model Coach
<article class="coach reveal">
    <div class="coach__avatar" aria-hidden="true">@Model.Initials</div>
    <h3>@Model.Name</h3>
    <p class="coach__role">@Model.Role</p>
    <p>@Model.Bio</p>
</article>
