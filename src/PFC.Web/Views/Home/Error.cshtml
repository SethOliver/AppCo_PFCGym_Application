@{
    ViewData["Title"] = "Something went wrong";
}
<section class="auth">
    <div style="text-align:center;max-width:460px">
        <div class="auth__logo" aria-hidden="true">PFC</div>
        <h1 style="font-size:32px">Something went wrong</h1>
        <p class="lead" style="margin-inline:auto">
            Sorry — that page could not be loaded. Try again, or head back to the home page.
        </p>
        <a class="btn btn--red" asp-controller="Home" asp-action="Index" style="margin-top:28px">Back to home</a>
    </div>
</section>
